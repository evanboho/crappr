<h2><%= @post.user.screen_name %></h2>
<br/>
<div class="big round">
  "<%= @post.content %>"
</div>
<br><nav class="edit_delete round">
<% if @post.user == current_user %>
<%= link_to 'edit', edit_post_path(@post) %>
<%= link_to 'delete', @post, confirm: 'Are you sure?', method: :delete %><br/>
<% end %>
<%= link_to 'all slams', posts_path %>
</nav>
<br/>
<hr>
<%= render 'comment_form' %>
<br/>
<% unless @post.comments.empty? %>
  <%= render 'comment'  %>
<% else %>
<% end %>