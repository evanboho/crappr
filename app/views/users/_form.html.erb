<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="signup_field round">
    <%= f.label :email, "email" %><br />
    <%= f.text_field :email %>
  </div>
  <div class="right">
	
    <%= f.submit "join" %>
  </div>
<% end %>
